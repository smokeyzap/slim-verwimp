<!doctype html>
<html lang="en">
    <head>
        <title>{{title}}</title>
        <!-- Required meta tags -->
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <link rel="shortcut icon" href="{{base_url()}}/public/logo/favicon.ico" type="image/x-icon">
        <!-- Bootstrap 3.3.7 -->
        <link rel="stylesheet" href="{{base_url()}}/public/css/bootstrap.min.css">
        <!-- Font Awesome -->
        <link rel="stylesheet" href="{{base_url()}}/public/css/font-awesome.min.css">
        <!-- Ionicons -->
        <link rel="stylesheet" href="{{base_url()}}/public/css/ionicons.min.css">
        {% if is_admin == 'yes' %}
        <!-- Theme style -->
        <link rel="stylesheet" href="{{base_url()}}/public/css/AdminLTE.min.css">
        <!-- Skin red -->
        <link rel="stylesheet" href="{{base_url()}}/public/css/skin-red.min.css">
        {% endif %}
        <!-- Font style -->
        <link href="https://fonts.googleapis.com/css?family=Montserrat:100,100i,200,200i,300,300i,400,400i,500,500i,600" rel="stylesheet">
        <!-- Toastr JS -->
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/toastr.min.css">
        <!-- DataTables -->
        <link rel="stylesheet" href="//cdn.datatables.net/1.10.19/css/jquery.dataTables.min.css">
        <!-- Custom style -->
        <link rel="stylesheet" href="{{base_url()}}/public/css/style.css">
    </head>
    <body>
        {% if user_id %}
            {% include 'layouts/navigation-menu.twig' %}
        {% endif %}
        
        <div class="container-fluid">
            <br>
            {% if user_id %}
                {# <h3 class="text-right">{{lang.label.office_tel}}: {{lang.label.office_number}}</h3> #}
                {# <h2 class="text-right">Order amount: <span id="txt_total_amount">0.00</span></h2> #}
            {% endif %}
            {% block content %}{% endblock %}
            
        </div>
        <br>
        <script src="{{base_url()}}/public/js/jquery.min.js"></script>
        <!-- Latest compiled and minified JavaScript -->
        <script src="{{base_url()}}/public/js/bootstrap.min.js"></script>
        <script src="//cdn.datatables.net/1.10.19/js/jquery.dataTables.min.js"></script>
        <script src="https://cdn.datatables.net/scroller/2.0.0/js/dataTables.scroller.min.js"></script>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/toastr.min.js"></script>
        <script src="{{base_url()}}/public/js/{{title | lower | replace({' ': '_'})}}.js"></script>
    </body>
</html>